# Smart Ledger ロードマップ

次に取り組む機能を決めるための一覧。実装済みの機能は README を、コード規約は CLAUDE.md を参照。
優先度は「毎月の家計簿として使い続けるうえで最初に困る順」で付けている。
実装を始めるときは該当項目の「データ構造への影響」を確認し、Excel の列追加がある場合は旧ファイルも読めるようにする。

最終更新: 2026-09-21

## 完了済み

| PR | 内容 |
| --- | --- |
| #1 | MVP 本体(CSV 取込・分類・Excel 永続化・Web UI・Dropbox コピー・Windows スクリプト) |
| #2 | レビュー対応(読み書きの直列化、バックアップ名の衝突回避、confidence の検証) |
| #3 | カテゴリ管理画面(追加・名称変更の伝播・並び替え・削除ガード、Jev 向け説明文) |
| #4 | Windows スクリプトの診断(ログ、Enter 待ち、.cmd ランチャー、レジストリからの Python 検出) |
| #5 | 加盟店キーによるルール照合(請求月を除いて一致)、Jev のカナ略称読み替えヒント |
| #6 | 二重起動ガード(同じポートで起動済みなら既存の画面を開く) |
| #7 | 取込の取り消し(import_id 単位で明細・内訳・履歴を削除。同じ CSV を再取込できる) |

## 運用確認で未実施

- [ ] Dropbox パスを設定して保存し、iPhone の Dropbox / Excel アプリで `latest/household.xlsx` の見え方(列幅・日本語・金額書式)を確認する
- [ ] 2 か月分以上を取り込んだ状態でダッシュボードの前月比・6 か月推移を実データで確認する

## 機能候補

優先度の高い順。「規模」は実装からテスト・README 更新までの目安。

### 1. 手動明細の追加・削除(規模: 中)

- 現金支出や他カードの支出を画面から登録・削除できるようにする。カード 1 枚分の CSV だけでは家計簿として漏れが出る
- データ構造への影響: `transactions` に列追加は不要。`import_id` は `manual`、`row_key` は空、`classification_source` は `manual` とする。`imports` シートには載せない
- 集計・要確認・ルール適用は既存の明細と同じ扱い。取込の取り消しの対象外(単体削除で消す)
- 画面: 明細一覧に「手動で追加」ボタン、編集画面に「この明細を削除」(手動明細のみ、または取込明細も許可するか要決定)

### 2. 内訳テンプレート(規模: 小)

- KYASH のように毎月分割する明細で、同じ加盟店キーの直近の内訳を「前回の内訳を複写」で埋める
- データ構造への影響: なし(`allocations` から直近の明細を検索して複写)
- 金額が違う月は差額を最後の行に寄せるか、比率で配分するかを選べると便利。まずは差額寄せで十分

### 3. 年間・カテゴリ推移レポート(規模: 中)

- 12 か月の月別総支出とカテゴリ別推移(表 + 簡易バー)、対象年の切替
- 月次集計・カテゴリ別集計の CSV / Excel エクスポート(集計は `aggregation.py` を再利用)
- データ構造への影響: なし
- 注意: 内訳(allocations)を使った二重計上防止のルールを年間でも守る

### 4. 複数 CSV 形式への対応(規模: 中〜大)

- カード会社ごとのパーサープロファイル(メタ行のキー、ヘッダー行の目印、列名候補、文字コード優先順)を定義し、CSV の内容から自動選択する
- データ構造への影響: `imports` にプロファイル名の列を追加すると追跡しやすい(任意)。`transactions.card` はカード名で区別できる
- 現在の `csv_parser.py` の列名候補タプルを、プロファイルの既定値として切り出す形にすると差分が小さい
- 重複判定は (row_key, カード名) なので別カードの同一明細は別扱いになる。プロファイル間で `card` の表記を揃えること

### 5. バックアップからの復元(規模: 小)

- `data/backup/` の世代一覧を画面に出し、選んだ世代を正本に戻す(戻す前に現在の正本も世代バックアップに退避)
- データ構造への影響: なし
- 注意: 復元中に他のリクエストが保存しないよう、`ExcelRepository.lock` の中で行う

### 6. 明細の単体削除(規模: 小)

- 編集画面から 1 件だけ削除する(内訳も同時に削除)。誤取込や返品の整理用
- データ構造への影響: なし
- 注意: 削除した明細の `row_key` が消えるため、同じ CSV を再取込すると再び新規として入る。削除を「除外」として `row_key` だけ残す設計にするなら `transactions` に `excluded` 列が必要(要検討)

## 対象外(MVP 方針を継続)

SQL / SQLite、Docker、WSL 前提、n8n、ユーザー認証、外部公開、Dropbox API、Dropbox → Smart Ledger の逆同期、
iOS ネイティブアプリ、銀行・カード API 連携、スクレイピング、複雑な予算管理、機械学習モデルの自前学習。

## 進め方

1. この一覧から 1 件選び、`feature/NN-<name>` ブランチを main から切る
2. 実装 → `ruff check .` / `ruff format .` / `tools/insert_block_blank_lines.py` / `pytest` → README 更新
3. `/pre-pr-review` でセルフレビューしてから PR を作成する
4. マージ後、この文書の「完了済み」に移し、候補の優先度を見直す
